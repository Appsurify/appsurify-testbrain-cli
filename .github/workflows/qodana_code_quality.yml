name: Qodana
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - development

jobs:
  qodana:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Qodana Scan'
        uses: jetbrains/qodana-python:latest
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN }}